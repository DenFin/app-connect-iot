{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxi dev",
    "generate": "nuxi generate",
    "preview": "nuxi preview",
    "postinstall": "nuxi prepare",
    "lint": "eslint --ext .js,.ts,.vue --ignore-path .gitignore .",
    "lint:fix": "eslint --ext .js,.ts,.vue --ignore-path .gitignore . --fix",
    "typecheck": "nuxi typecheck",
    "prepare": "husky install",
    "changelog": "npx changelogen@latest --bump",
    "test": "pnpm postinstall && vitest run",
    "test:coverage": "pnpm postinstall && vitest run --coverage",
    "test:watch": "pnpm postinstall && vitest --watch",
    "cy:open": "start-server-and-test 'pnpm dev' http-get://localhost:3000 '$(pnpm bin)/cypress open'",
    "cy:run": "start-server-and-test 'pnpm dev' http-get://localhost:3000 'npx cypress run --browser chrome --headless --reporter junit'",
    "cy:run:ci:chrome": "pnpm dev & sleep 10 && curl -i http://localhost:3000/test ; npx cypress run --browser chrome --headless --reporter junit",
    "cy:run:ci:chrome:mobile": "pnpm dev & sleep 10 && curl -i http://localhost:3000/test ; npx cypress run --browser chrome --headless --reporter junit --config viewportWidth=428,viewportHeight=926",
    "cy:run:ci:firefox": "pnpm dev & sleep 10 && curl -i http://localhost:3000/test ; npx cypress run --browser firefox --headless --reporter junit",
    "cy:run:ci:firefox:mobile": "pnpm dev & sleep 10 && curl -i http://localhost:3000/test ; npx cypress run --browser firefox --headless --reporter junit --config viewportWidth=428,viewportHeight=926",
    "cy:run:ci:safari": "pnpm dev & sleep 10 && curl -i http://localhost:3000/test ; npx cypress run --browser webkit --headless --reporter junit",
    "cy:run:ci:safari:mobile": "pnpm dev & sleep 10 && curl -i http://localhost:3000/test ; npx cypress run --browser webkit --headless --reporter junit --config viewportWidth=428,viewportHeight=926"
  },
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "15.0.0",
    "@bahmutov/cypress-esbuild-preprocessor": "2.1.5",
    "@commitlint/cli": "17.3.0",
    "@commitlint/config-conventional": "17.3.0",
    "@cypress/webpack-preprocessor": "5.15.6",
    "@nuxtjs/eslint-config-typescript": "^12.0.0",
    "@nuxtjs/tailwindcss": "^6.1.3",
    "@testing-library/jest-dom": "5.16.5",
    "@testing-library/vue": "6.6.1",
    "@vitejs/plugin-vue": "3.2.0",
    "@vitest/coverage-c8": "0.25.2",
    "@vue/test-utils": "2.2.4",
    "cypress": "11",
    "eslint": "^8.28.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-tailwindcss": "^3.7.0",
    "eslint-plugin-vue": "^9.7.0",
    "husky": "^8.0.0",
    "jsdom": "20.0.3",
    "nuxt": "3.0.0",
    "playwright-webkit": "1.28.1",
    "start-server-and-test": "1.14.0",
    "typescript": "^4.9.3",
    "unplugin-auto-import": "0.11.4",
    "vitest": "0.25.2",
    "vue-tsc": "^1.0.9"
  },
  "dependencies": {
    "@kernpunkt/bega-component-library": "1.0.1",
    "@sidebase/nuxt-auth": "0.3.0",
    "@nuxtjs/i18n": "8.0.0-beta.7",
    "@pinia/nuxt": "0.4.5"
  }
}
